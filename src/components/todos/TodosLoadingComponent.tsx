import { LoadingSpinner } from '@/components/LoadingSpinner';

export function TodosLoadingComponent() {
  return (
    <LoadingSpinner
      title="Smart Study Planner"
      description="Loading your tasks..."
    />
  );
}
